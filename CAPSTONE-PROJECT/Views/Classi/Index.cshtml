@model IEnumerable<CAPSTONE_PROJECT.Models.Classi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Anno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sezione)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Anno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sezione)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdClasse }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdClasse }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdClasse })
        </td>
    </tr>
}

</table>
