@model IEnumerable<CAPSTONE_PROJECT.Models.AlunniListaAttesa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DomandeIscrizione.NomeAlunno)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DomandeIscrizione.NomeAlunno)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAlunnoLista }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAlunnoLista }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAlunnoLista })
        </td>
    </tr>
}

</table>
